# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A zero-build browser app — a real-time meeting assistant for a Thai dev team. The user listens to a meeting via Web Speech API, optionally shares their screen, and gets streamed answers from an LLM. UI text is Thai; the target user is a Go / PostgreSQL / React+TypeScript backend dev (stack is configurable, not hardcoded).

**Capturing meeting audio (the core use-case):** the recognizer/recorder only hears the **mic**, so other people's voices (out the speakers / via Zoom·Meet·Teams) don't reach it. On macOS the fix is to route system audio → a virtual input with **BlackHole** (Multi-Output Device so you still hear it, then set browser/app input = BlackHole, or an Aggregate Device to capture meeting + your own mic together). `getDisplayMedia` cannot grab full system audio on macOS (tab audio only), so BlackHole is the only path that covers native Zoom/Teams. The home screen has a helper card + a **ตรวจอุปกรณ์เสียง** button (`checkAudioDevices`, js/03) that checks for an installed virtual device. Full setup steps live in `README.md`.

**No build step / bundler.** `index.html` (HTML shell + the small Tailwind config inline `<script>`), `styles.css` (all component CSS), and the app logic split into **ordered classic scripts** under `js/` — loaded in numeric order, sharing one global scope (top-level `const`/`let`/`function` are globals; cross-file refs resolve at call-time, so load order only matters for top-level *executed* code, which is arranged so each file's deps load before it):

- `js/01-config.js` — `PROVIDERS`, `QA_SYSTEM`/`EST_SYSTEM`, `stackLine`
- `js/02-state.js` — `store`/`skey`/`getKey`, state vars, DOM refs (`$`), model-select helpers, `loadProvider`
- `js/03-models.js` — `modelsReq`, `fetchModels`, provider/model handlers, `loadProvider()` call, `checkAudioDevices`
- `js/04-voice.js` — mode/`setMode`, toggles, `refreshStatus`, screen share, voice listen toggle (`startWebListen`/`stopWebListen`/`voiceSend`; `stopListen` = dead/compat)
- `js/05-llm.js` — `buildRequest`, `streamLLM`
- `js/06-render.js` — `el`/`esc`/`highlightCode`/`inline`/`mdToHtml`/`renderEstimate`, chat bubbles, voice preview, `looseJSON`
- `js/07-main.js` — `submit`, sessions/routing, font, init IIFE
- `js/08-overlay.js` — หน้าต่างลอย (floating overlay) ที่ mirror `#results` (transcript + คำตอบ AI) + มี control bar. **2 environment:** (1) **Electron desktop** (`IS_ELECTRON = window.electronAPI?.isElectron`) → หน้าต่างลอยแยก always-on-top จริง (เห็น desktop ทะลุ; ความจางคุมด้วย CSS bg-alpha `--ov-a` ฝั่ง overlay.html ไม่ใช่ window opacity; ซ่อนจากแชร์จอด้วย `setContentProtection` — **default ON**) — `openElectron`/`closeElectron` คุยกับ main process ผ่าน IPC (`electronAPI.openOverlay`/`pushOverlayHTML`/`pushOverlayControls`/`onOverlayAction`/`onOverlayClosed`); (2) **Browser** → Document PiP (fallback เดิม, `openPip`/`closePip`, คัด stylesheet ผ่าน `copyStylesTo`). mirror logic ร่วม: `startMirror(sink)`/`syncOverlay` (coalesce ด้วย rAF)/`stopMirror` — `sink` เป็น DOM (PiP) หรือ push ผ่าน IPC (Electron). เปิดตอนกด `startBtn` (`openOverlay`), ปิดตอนกลับ home (`closeOverlay`); ปุ่ม `🪟 #floatBtn` ใน chat header = toggle (`toggleOverlay`/`updateFloatBtn`/`isOverlayOpen`). control bar (`.fc-mic`/`.fc-stop`/`.fc-screen`/`.fc-input`/`.fc-send`) proxy ไปปุ่มหลัก (`micBtn`/`stopBtn`/`screenBtn`) + `submit()`; `wireFloatControls` ผูก event (PiP), `syncFloatControls` ดัน label/state จากปุ่มหลัก (Electron=`pushOverlayControls`, PiP=เขียน DOM) ขับด้วย `ctrlObserver`. ฝั่ง Electron: หน้าต่างลอยส่ง action กลับ (`sendAction` → `onOverlayAction`); ส่ง `"ready"` ตอนโหลดเสร็จ → main paint html+controls เริ่มต้น + ยืนยัน `elecOpen` (authoritative; `openElectron` ตั้ง `paintNow=false` ไม่ eager-push กัน race; `onOverlayClosed`/`did-fail-load` แก้ `elecOpen` ไม่ให้ค้าง). **Electron security:** `sandbox:true`+`contextIsolation`+`nodeIntegration:false` ทั้ง 2 window, CSP (`connect-src` เฉพาะ provider hosts กัน key exfil; overlay `connect-src 'self'`), `setWindowOpenHandler` deny + block `will-navigate`, permission allowlist เฉพาะ `media`, IPC self-control channels validate sender = overlayWin จริง, screen-share fallback gate `userGesture`. **(loads last)**

No framework, no runtime deps สำหรับ browser path. Serve over `http://` (Chrome/Edge — required for `SpeechRecognition`; media APIs blocked on `file://`); LLM calls go directly from the browser to the provider. Optional `config.local.js` (loaded before `js/*`) sets `window.__MA_KEYS__` for local key auto-fill.

**Desktop app (Electron) — optional wrapper:** `package.json` + `electron/` (`main.js` = สร้าง main window โหลด `index.html` + overlay window แยก always-on-top; `preload.js` = contextBridge `window.electronAPI`; `overlay.html` = renderer ของหน้าต่างลอย — header slider ความจาง/📌 always-on-top/🛡 content-protection/✕ + `#overlayResults` + `.float-composer`; `picker.html` = source picker สำหรับแชร์จอ). โครง = main window (แอปเต็ม) + overlay window (mirror ผ่าน IPC). **แชร์จอ:** `setDisplayMediaRequestHandler` + `useSystemPicker:true` — macOS 15+ ใช้ OS picker; Windows/macOS เก่า (handler ถูกเรียก) → `pickSource()` เปิด picker window (`picker.html`) ให้เลือก screen/หน้าต่าง (grid thumbnail จาก `desktopCapturer.getSources`) → `callback({video})` | ปิด/ยกเลิก = deny. แก้ปัญหาหลายจอ + เลือกหน้าต่างเดียวให้ overlay ไม่ติดฟีด. `npm install` แล้ว `npm start` (dev) / `npm run dist` (build `.dmg`/`.app`). **Electron quirk:** Web Speech `SpeechRecognition` ใช้ใน Electron ไม่ได้ (error `network`) → js/04-voice บังคับ `sttEngine="gemini"` + disable ปุ่ม `#sttWeb` เมื่อ `window.electronAPI`. โค้ด Electron ทั้งหมด gate ด้วย `window.electronAPI` → รันใน browser ปกติไม่ regress (ใช้ PiP). `node_modules/`+`dist/` gitignored.

## Architecture

Key pieces (file in parens):

- **Multi-provider LLM abstraction** (`PROVIDERS`, `buildRequest`, `modelsReq`, `streamLLM`): the one part worth understanding. `buildRequest(provider, key, model, opts)` returns `{url, headers, body, extract}` normalizing four provider APIs (OpenRouter, Gemini, OpenAI, Claude) that have *different* request shapes, auth headers, image-embedding formats, and SSE response schemas. `extract(parsedJSON)` pulls the text delta out of each provider's distinct stream chunk. `buildRequest` takes a `think` opt (from the `🧠 คิดก่อนตอบ` toggle `thinkOn`, persisted `ma_think`, default **off**): when off it disables model reasoning — gemini `generationConfig.thinkingConfig.thinkingBudget:0`, openrouter `reasoning:{enabled:false}` (claude sends no thinking anyway; openai left as-is). qa/est pass `think:thinkOn`; STT stays no-think. `buildRequest` also takes a **`history=[]`** opt (multi-turn / ถามต่อได้): `submit()` builds it from `curSess.items` (last 10 Q&A pairs, gated by the `🧵 จำบทสนทนา` toggle `contextOn`/`ma_context`, default **on**) and each provider splices prior `{role,text}` turns before the current user turn — openai/openrouter & claude `messages`, gemini `contents` (role `"model"` not `"assistant"`); claude keeps its json `{` prefill LAST. History is text-only (images are current-turn only). `streamLLM` is provider-agnostic — it reads the SSE `data:` lines and calls `extract` per chunk; it also accumulates **token usage** via each request's `usage(chunk)` parser into `req.usageAcc` ({in,out}) — gemini `usageMetadata`, claude `message_start`/`message_delta`, openai/openrouter final chunk (`stream_options:{include_usage:true}`). `submit()` reads `req.usageAcc` → `addCost()` attaches a `$` cost **only for OpenRouter** (`modelPrice()` uses the real `pin`/`pout` $/token captured into `modelsList` by `modelsReq` from OpenRouter's `pricing`; other providers have no per-model pricing so token-only) → stores `item.tok` ({in,out,cost?}) + shows a per-answer `.tok` badge (`tokBadge`/`fmtTok`/`fmtCost` in js/06) under the bubble; `updateTokTotal()` sums `curSess.items[].tok` (+cost) into the header `#tok`. `modelsReq(provider, key)` is the parallel abstraction for the `GET …/models` endpoints — its `extract(json)` returns a vision-filtered list of model ids that the ↻ button loads into the model `<select>` (cached in `localStorage` as `ma_models_<provider>`; falls back to `PROVIDERS[p].models` presets on fetch failure). **To add a provider, edit `PROVIDERS`, `buildRequest`, and `modelsReq`.**
- **Two modes** driven by `mode` state: `qa` (free-form answer, streamed token-by-token into a card) and `est` (work estimation — forces JSON output via the provider's json mode, parsed by `looseJSON` and rendered by `renderEstimate`). Each mode has its own system prompt: `QA_SYSTEM`, `EST_SYSTEM`. These set tone and are polyglot (no hardcoded stack). **Stack/context is a configurable field** (`#stackCtx` on home, persisted `ma_stack`, default empty) — `stackLine()` appends it to the qa/est system prompt at submit; empty = no pinned language/stack (answer per the question), filled = bias toward that stack but follow the question if it names another. **Mode is chosen on the home/lobby before starting (the `#homeModes` `.mode-card` picker → `homeMode`; `startBtn` calls `setMode(homeMode)` then `newSession()`) and is LOCKED per session — there are no composer tabs.** The chat header shows the current mode via the `#curMode` badge (`MODE_LABEL`). `setMode(m)` updates `mode`, the badge, mic label, send-button text, and input placeholder. The session's mode is persisted (`curSess.mode` in `ensureSession`/`saveItem`) and restored on reload (`initSessions` → `setMode(curSess.mode||"qa")`).
- **Quick-ask + follow-up chips** (`QUICKASKS` js/02, `genFollowups`/`renderFollowups`): the floating composer shows `QUICKASKS` preset chips (click = send instantly); after a `qa` answer, `genFollowups()` (js/07) asks the LLM for 3 short follow-up questions (gated by `💡 แนะนำคำถามต่อ` `followupOn`/`ma_followup`, default on) rendered as chips under the answer bubble. Both use `.chip-ask[data-ask]` with one delegated handler in the float (PiP → `submit()` directly; Electron → `sendAction("ask")` → `submit`), so clicks ride the multi-turn `history`. Follow-up chips live in `#results` and mirror to the float automatically; quick chips are pushed to the Electron overlay via `pushOverlayControls` (`st.quick`) / built inline in PiP.
- **Voice loop** (`buildRec`, `startRec`, `voiceSend`): continuous `SpeechRecognition`; on final transcript + auto-send, a silence timer (`silenceMs`) triggers `voiceSend`. Listening is a **2-state toggle mirroring the gemini mode** (`micOn` + `setMicUI`): idle (`🎤 เริ่มฟัง`) ⇄ listening (`⏹ หยุด` + `✂️ ส่งเลย` `#stopBtn`). `startWebListen()`/`stopWebListen()` start/stop the recognizer **non-terminally** — stop keeps `finalText`+preview and does NOT lock the composer; pressing start again resumes/accumulates (buffer is cleared by `voiceSend` on send and by `resetListen` on newSession). The `#stopBtn` (`✂️ ส่งเลย`) calls `voiceSend()` to send the current `finalText` immediately without waiting for silence, then keeps listening (`if(micOn) startRec()`). `resetListen()` (called by `newSession`) returns to idle. `onend` restarts only while `micOn`. **(`stopListen()` — the old terminal `⏹ จบ` that set `stopped=true` + `lockComposer` view-only — has no button anymore; it and the `stopped`/`lockComposer`/`#roNote` + `initSessions` restore path are kept only for backward-compat with old persisted `curSess.stopped` sessions.)** The recognizer is stopped during the LLM call and restarted in `onend` to avoid feedback. The transcribed text is shown **live as a chat bubble** (`updateVoicePreview`/`clearVoicePreview` → `.voice-live` preview, "🗣 คนถาม (กำลังพูด…)"), NOT in the textarea — accumulated in `finalText`; `submit()` falls back to `finalText` when the textarea is empty (typed text wins). The preview is cleared on submit / mic-stop / newSession / clear.
- **STT engine toggle** (`sttEngine`, persisted `ma_stt`, default `web`): home settings seg (`#sttWeb`/`#sttGemini`/`#sttSoniox`).
  - **`soniox` = Soniox realtime (live ทีละคำ, ไทย+อังกฤษ code-switch)** via `toggleSonioxRecord`/`stopSoniox`/`sonioxFlush` (js/04-voice.js): getUserMedia → `AudioContext`+`ScriptProcessorNode` → PCM `s16le` → stream ผ่าน **WebSocket** `wss://stt-rt.soniox.com/transcribe-websocket` (config `model:"stt-rt-v5"`, `audio_format:"pcm_s16le"`, `sample_rate=ctx.sampleRate`, `language_hints:["th","en"]`, `enable_endpoint_detection:true`). non-final tokens → live preview, final tokens สะสมใน `sxFinal`; token `{"text":"<end>"}` = endpoint → `sxCommit()`. **`sxCommit` ไม่ส่งทันที** — มี debounce-merge (`sxPending`/`sxSendTimer`/`sxMergeMs()` = `max(silenceMs,2500)`): รวม utterance ที่ endpoint ตัดติดๆ กัน (คนหยุดคิดสั้นๆ) เป็นก้อนเดียว ส่งเมื่อเงียบจริงครบ `sxMergeMs`; พูดต่อก่อนหมดเวลา = ยกเลิก timer แล้ว merge ต่อ. ส่งจริงผ่าน `sonioxFlush()` (hands-free, reuse `hybridQ`/`_drainHybridQ`). `✂️ ส่งเลย` = `sonioxFlush` manual (ข้าม debounce). ต้องมี Soniox key (`#sonioxKey` → `getKey("soniox")`, sessionStorage; row reveal ใน `syncGeminiKeyRow`). **ทำงานใน Electron** (WS+getUserMedia+WebAudio ได้ — ต่างจาก web Speech ที่พัง). เสียเงินตามใช้. **แหล่งเสียง** (`audioSrc`, persisted `ma_audiosrc`, default `mic`; seg `#asrcMic`/`#asrcSys` ใน `#audioSrcRow` โผล่เฉพาะ Electron): Soniox อ่าน stream ผ่าน `getSttStream()` — `mic` = getUserMedia, `system` = **Electron loopback** (เสียงที่ออกลำโพง/ประชุม ไม่ต้อง VB-CABLE): renderer ส่ง `electronAPI.prepLoopback()` → main ตั้ง one-shot flag → `setDisplayMediaRequestHandler` grant จอหลัก+`audio:"loopback"` (ไม่เปิด picker) → renderer ทิ้ง video track เก็บแต่ audio. Windows/Electron เท่านั้น (macOS ใช้ BlackHole); `web`/`gemini` VAD ผูกกับ mic จึงคงใช้ mic.
  - In **Electron**, `#sttWeb` ถูกซ่อน (Web Speech พัง) → เหลือ `gemini` + `soniox`. (Gemini Live realtime STT was removed — v1beta WS auto-detects language, so Thai+English code-switching came out wrong-language/garbled; the batch `gemini` mode below is the accurate path.) `web` = Web Speech (live, the state machine above). `gemini` = **สด+AI auto-cut** (hands-free continuous) via `toggleGeminiRecord` (js/04-voice.js): press once `🎤 เริ่มฟัง` → `⏹ หยุด`. It runs `MediaRecorder` (per-utterance clip) plus a **VAD** that cuts each clip after `silenceMs` of silence by calling `cutHybridClip()` (which `mediaRec.stop()`s the current clip). **The VAD source depends on environment** (`VAD_AUDIO = window.electronAPI?.isElectron`): in **browser** it's Web Speech (`hybridRec=true`, `startRec()`) — `rec.onresult` shows the live preview AND drives the silence timer; in **Electron** (Web Speech broken) it's an **energy VAD over Web Audio** (`startVad`/`stopVad`): an `AnalyserNode` measures RMS on a `setInterval(…,100)` loop (deliberately NOT `requestAnimationFrame`, so it keeps firing when the window is backgrounded) and calls `cutHybridClip()` once RMS stays below the threshold for `max(silenceMs,1500)`ms. No live word-preview in the Electron path (just "🗣 กำลังพูด…"). `_hybridClipDone` then immediately `startHybridClip()`s the next clip (no gap), decodes the finished clip to mono WAV (`blobToWav`) and POSTs it as inline audio to `transcribeAudio` (js/05-llm.js) for an accurate transcript, and **always sends** it (hands-free) through a small serial queue (`hybridQ`/`_drainHybridQ`, guards the `busy` flag so utterances during an in-flight answer aren't dropped). Falls back to the Web Speech text (`hybridText`) if it fails. Stopping cuts+sends the final clip and closes the stream. **`transcribeAudio` picks its backend via `sttBackend()` (js/02): `provider==="openrouter"` with an OpenRouter key → POST to OpenRouter `chat/completions` with `model:"google/gemini-2.5-flash"` and the audio as an `input_audio` (base64 wav) content part — uses the user's OpenRouter credit, NO separate Gemini key; otherwise Gemini-native `generateContent` (needs a Gemini key — `provider==="gemini"` uses the main key field, others use the separate `#geminiKey` field that `syncGeminiKeyRow` reveals).** Shared verbatim/anti-hallucination prompt = `STT_PROMPT`; both paths post-strip a leading conversational preamble. The mode is gated by `sttBackend().key` (blocked + hinted if absent). Hands-free + accuracy; per-utterance transcribe lag (~1-2s after silence) and one audio call per utterance.
- **Screen share** (`getDisplayMedia` → hidden `<video id="screenVideo">` → `captureFrame`): grabs a JPEG frame (downscaled to max 1280px) only at submit time when sharing is on, base64-encoded and attached to the request as an image. All four providers are vision-capable.
- **Layout & sessions (lobby + floating chat)**: no sidebar/navbar. **The chat lives ONLY in the floating window** (Electron always-on-top window / browser Document PiP, see js/08). The main window shows just two visible views toggled by `showView(name)`: `#viewHome` (lobby — `.lobby-head` title + `⚙ #settingsBtn` gear → settings; `▶ เริ่ม` `startBtn` start card + `🪟 #resumeBtn` reopen-float button (shown when `curSess` exists) + `#homeList` session list, where the active session card gets a `● กำลังใช้` `.cur-badge`/`.active`), and `#viewSettings` (**dedicated settings page** — provider/model/key + STT + **คีย์ลัด/hotkeys** + `#advSettings`). `#viewCurrent` is **headless** (`display:none`, forced in `showView`) — it is NOT a visible view; it's the mirror source (`#results`) + the proxy targets for the float's controls (`#micBtn`/`#stopBtn`/`#screenBtn`/`#input` + `submit()`), plus the hidden writers `#error`/`#status`/`#empty`/`#count`/`#tok`/`#curMode`/`#curTitle` that the float reads via `syncFloatControls`. **Feedback visibility:** since `#error`/`#status` are headless, `showError()` also fires a `#toast` (top-center, always visible) and `syncFloatControls` mirrors error/status into the float (overlay `#ovErr`/`#ovStatus`, PiP `.pip-err`/`.pip-status`). `openSession(id)`: if `id===curId` → reopen live float (readonly if `curSess.stopped`); else render the past session into `#results` and open the float **readonly** (composer hidden, `🔒 ดูอย่างเดียว`). UI is minimal/flat (no decorative accent bars/icon pills/nested cards; quiet teal-tint selected states; secondary hint copy demoted to `title=` tooltips). Conversation renders as **chat bubbles, all left-aligned** with a role label (`roleLabel()` 🗣 คนถาม / 🤖 AI) — `addUserMsg()` (teal bubble) + `addAiMsg()` (panel bubble, holds `.ans`); `scrollBottom()` after each. All chat controls live in the **floating window**: a toolbar (🖥 screen + ⚡ auto-send + ไทย/Eng lang seg — lang shown only for `web` STT) with title/stat in the header, and a composer row (🎤 mic + ✂️ ส่งเลย + input + ➤ send). They proxy to the headless `#viewCurrent` buttons; mode is fixed per session, picked on home. **Hotkeys** (js/09-hotkeys): per-action shortcuts (start/stop listen · send-now · share/stop screen · toggle auto) recorded from physical `e.code` (English keyboard only, require a modifier); Electron registers `globalShortcut` (works unfocused), browser fires on focused-window keydown. Floating window size is remembered (Electron bounds → userData file; PiP size → localStorage). **Sessions persist in IndexedDB** (db `ma_db`, store `sessions`, keyPath `id`) — larger quota than localStorage (no ~5MB cap). Pattern: an in-memory map `SESS` (id→session) is the sync source of truth; `loadSess`/`sessIndex` read it synchronously, `persistSess`/`deleteSession` write memory + fire-and-forget `idbPut`/`idbDel` (helpers in js/02). `initSessions` is async — `await idbReady` → `SESS = await idbGetAll()` → one-time `migrateFromLocalStorage()` (imports legacy `ma_sess_*` keys if IDB is empty; old localStorage left as backup). `sessIndex()` is derived from `SESS` (no separate index key). If IndexedDB is unavailable, `idb*` no-op (sessions degrade, don't break). A session is `{id,title,mode,createdAt,updatedAt,provider,model,stopped?,items}`; an `item` is `{q,mode,hadImage,raw}` (raw = markdown / `JSON.stringify(est)`), rebuilt by `renderSessionInto` (chronological). `submit()`→`saveItem()`; `newSession`/`deleteSession`(+confirm)/`openSession`/`initSessions` (always lands on `home`/settings by default, restoring the last session into memory so clicking it reopens the chat). Settings live on the dedicated `#viewSettings` page (reached via the lobby `⚙` gear). Font size = `--fs` slider, persisted `ma_fontsize`.
- **Tailwind**: loaded via Play CDN with `corePlugins.preflight:false` (so it does NOT reset the existing component CSS — `.ans`/markdown/forms/cards stay intact) and brand colors mapped in `tailwind.config`. Tailwind utilities are used for shell polish (gradient title, hover/shadow/transition); the component styling stays in `styles.css`. Needs internet on page load.
- **Persistence**: chat sessions → **IndexedDB** (see above). Settings — selected provider, per-provider model, fetched model cache, `ma_cur`/`ma_silence`/`ma_stt`/`ma_fontsize` — stay in `localStorage` (`ma_*` keys) via the `store` helper (small, sync). **API keys are kept in `sessionStorage` via the separate `skey` helper** (cleared when the tab closes); `getKey(p)` migrates any key found in legacy `localStorage` into `sessionStorage` and purges it. Keys never leave the browser except to the chosen provider. Claude calls use `anthropic-dangerous-direct-browser-access` to allow direct browser-to-API requests (CORS).

## Conventions

- UI copy and comments are Thai; identifiers and code are English.
- No external libs — keep it dependency-free and **no build step** (`index.html` + `styles.css` + `js/0N-*.js` ordered classic scripts). Don't add a bundler/framework unless there's a strong reason. Component styling goes in `styles.css`; app logic in the matching `js/` file. When adding a top-level call, make sure its deps live in an earlier-numbered file (or are function decls called only at runtime).
- DOM access via the `$(id)` helper; elements built with `el(tag, cls, html)`; always `esc()` user/LLM text before `innerHTML`.
